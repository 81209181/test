<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkt.btu.sd.core.dao.mapper.SdTicketFileUploadMapper">

    <resultMap id="entity" type="com.hkt.btu.sd.core.dao.entity.SdTicketUploadFileEntity">
        <result column="FILE_NAME" property="fileName" />
        <result column="FILE_UPLOAD" property="content" />
        <result column="CREATEDATE" property="createDate"/>
        <result column="CREATEBY" property="createBy"/>
    </resultMap>

    <insert id="insertUploadFile">
        insert into TICKET_FILE_UPLOAD (TICKET_MAS_ID,FILE_NAME,FILE_UPLOAD)
        values (#{ticketId},#{fileName},#{file})
    </insert>

    <select id="getUploadFiles" resultMap="entity">
        select file_name,file_upload from TICKET_FILE_UPLOAD where ticket_mas_id =#{ticketMasId}
    </select>

    <delete id="removeUploadFileByTicketMasId">
        delete from TICKET_FILE_UPLOAD where TICKET_MAS_ID = #{ticketMasId}
    </delete>
</mapper>
