<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkt.btu.sd.core.dao.mapper.SdCronJobLogMapper" >

    <insert id="insertLog">
        insert into CRON_JOB_LOG (SERVER_IP, JOB_GROUP, JOB_NAME, JOB_CLASS, ACTION, CREATEBY)
        values (#{serverIp}, #{jobGroup}, #{jobName}, #{jobClass}, #{action}, #{createby})
    </insert>


</mapper>
