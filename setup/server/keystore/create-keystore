Reference: https://dzone.com/articles/aes-256-encryption-java-and

// useful tool: http://keystore-explorer.org/downloads.html

Create JCE Key Store (JCEKS)
    1. open cmd
    2. go to java home -> bin
    3. input following command with [input]
        - [input]:
            [input_keystore_filename]       filename of the keystore                store on server
            [input_storepass]               passphrase to access the keystore       inject to jvm instance (CRITICAL: must be >=128bit, https://crypto.stackexchange.com/questions/20374/what-java-actually-stores-inside-keystore-when-generating-keys)
            [input_keypass]                 passphrase to access a key              store in source code (dummy)
            [input_alias_int]               alias of a key (int)                    store in the beginning byte each encrypted data entity in db

        keytool -genseckey -keystore [input_keystore_filename].jck -storetype jceks -storepass [input_storepass] -keyalg AES -keysize 256 -alias [input_alias_int] -keypass [input_keypass]

        UAT Example:
        keytool -genseckey -keystore aes-keystore.jck -storetype jceks -storepass 7284ec9a-11ab-4999-936f-7f823376bd37 -keyalg AES -keysize 256 -alias 1 -keypass servicedesk

List Key profile (without actual key) JCEKS files
    1. input following command in cmd
        keytool -list -keystore aes-keystore.jck
    2. input keystore storepass